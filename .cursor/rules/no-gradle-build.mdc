---
description: Не запускать Gradle-сборку — только код
alwaysApply: true
---

# Без Gradle-сборки

Никогда не запускай `./gradlew` или любые Gradle-команды (`assembleDebug`, `build`, `test` и т.п.).

Сборку и тестирование делает пользователь самостоятельно.

Задача агента — только писать код.

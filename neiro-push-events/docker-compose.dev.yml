services:
  neiro-push-events:
    build: .
    container_name: neiro-push-events-dev
    env_file:
      - .env
    ports:
      - "127.0.0.1:8011:8011"
    volumes:
      - ./app:/app/app
      - ./templates:/app/templates
      - ./scripts:/app/scripts
      - ./data:/data
    command: [
      "uvicorn", "app.main:app",
      "--host", "0.0.0.0", "--port", "8011",
      "--reload", "--reload-include", "*.html",
    ]

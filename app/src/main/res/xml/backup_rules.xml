<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- DataStore: профиль, календарь, тема -->
    <include domain="file" path="datastore/" />
    <!-- Токены YClients — только в EncryptedSharedPreferences, не бэкапим -->
    <exclude domain="sharedpref" path="neiro_yclients_secure.xml" />
    <exclude domain="sharedpref" path="neiro_yclients_fallback.xml" />
</full-backup-content>

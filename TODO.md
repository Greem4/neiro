# Дорожная карта

Актуально на 13.08.2026. Готовое отмечено `[x]`, в работе — `[~]`,
запланировано — `[ ]`.

Ближайшая работа — [`docs/next/`](docs/next/README.md): скриншоты и крайние
случаи обновления. Здесь — карта целиком.

## Сделано

Крупные куски, закрытые целиком. Развёрнуто — по ссылкам, здесь только чтобы
помнить, что оно есть.

<details>
<summary>Приложение: календарь, архив, деньги, уведомления</summary>

- **Боковая панель** — drawer профиля, жесты, BackHandler
  ([`docs/profile-drawer.md`](docs/profile-drawer.md))
- **Графики в профиле** — столбцы + линия, тап по месяцу → занятия и сумма
- **Мини-календари** — оверлей выбора месяца (`MonthPickerOverlay`)
- **Архив** — режим и переключатель с YClients, перенос дня, снятие с архива,
  индикатор расхождения с API, архивные in-app уведомления, офлайн-правка
  статусов, экспорт и импорт JSON
- **YClients** — auth, sync, live-опрос, автосинхронизация при открытии
- **Уведомления** — напоминания, дайджесты, настройки, deep link в день
- **Профиль** — регистрация, цены, налог, зарплата, годовая статистика
- **Настройки** — тема, прибыль, уведомления, автосинхронизация
- **Безопасность бэкапа** — DataStore в бэкапе, токены YClients исключены
- **Ставки и деньги из API** — история ставок по месяцам, факт из зарплат
  YClients, заморозка прошлых месяцев, разбор расхождений
  ([`docs/pricing-from-api/`](docs/pricing-from-api/README.md))

</details>

<details>
<summary>Релизы и самообновление — работает, проверено на телефоне</summary>

Версия из `version.properties`, релиз по тегу `vX.Y.Z` с подписанным APK,
суммой и сжатым mapping, проверка новой версии раз в сутки, загрузка со
сверкой суммы и подписи, установка через `PackageInstaller`, экран
«О программе». Публичное лицо репозитория: MIT, CONTRIBUTING, SECURITY,
шаблоны, CHANGELOG.

С 13.08.2026 выпуск полностью автоматический: слияние PR поднимает версию,
ставит тег и выпускает релиз, заметки берутся из `CHANGELOG.md`. Проверено
делом — `0.1.6` вышла и доехала до телефона без единого ручного шага; mapping
в релизе сжат, 51,44 → 3,66 МиБ.

Документы — [`docs/updater/`](docs/updater/README.md), порядок выпуска —
[`RELEASE.md`](docs/updater/RELEASE.md#как-выпустить-версию).

</details>

<details>
<summary>Сервис <code>neiro-push</code> — переход завершён 13.08.2026</summary>

Токены YClients живут только на Pi, телефон ходит через прокси `/v1` на порту
8012: вход по логину и паролю, `device_token`, прокси семи эндпоинтов, лимиты
попыток, отзыв устройств, лента событий с ack курсора. Приложение работает без
связи на сохранённых данных.

Оба прежних поколения погашены и удалены из репозитория: `server/` —
02.08.2026, `neiro-push-events/` — 13.08.2026, код остался в истории git.

Документы — [`docs/neiro-push/`](docs/neiro-push/README.md), порядок перехода —
[`ROLLOUT.md`](docs/neiro-push/ROLLOUT.md).

</details>

## В работе

- [~] **Статусы занятий** — парсинг и отображение из YClients готовы, ручная
      смена в архиве работает. Осталось: быстрый тап «пришёл / не пришёл» на
      карточке дня, без входа в диалог и только в архиве
- [~] **Основной календарь** — сетка, выделение, сводка дня и тулбар готовы.
      Осталось: плотность имён в ячейке, жесты, accessibility
- [~] **README карточкой проекта** — требования и скрипт готовы
      ([`docs/screenshots.md`](docs/screenshots.md)), кадров нет

## Планы

### Карточки детей

Крупная фича, взята на потом. Данные считаются из того, что уже есть:
`effectiveDayData` + `SessionParser`, заготовка `computeRecentStudents` в
`CalendarViewModel`, переход из профиля стоит выключенной заглушкой.

- [ ] Экран списка учеников
- [ ] Карточка ребёнка: посещения / пропуски / отмены
- [ ] График посещаемости по месяцам
- [ ] Сумма принесённых денег
- [ ] Краткая история (даты, дни недели)
- [ ] Поиск ученика по имени — делается вместе с карточками, отдельно смысла нет

### Групповые занятия по `activity_id`

Нейройога, подгрупповые, «Мы вместе» сейчас не ловятся: групповым считается то,
где в названии есть слово «интенсив». Правильный признак — `activity_id` у
записи, он уже приходит в `RecordData`. Решено делать, но не срочно.

### Тесты обновления

Крайние случаи проверяются кодом, а не руками на телефоне — гоняются в CI на
каждый push.

- [ ] Подменённая сумма — `UpdateVerifier` возвращает отказ, файл удалён
- [ ] Обрыв сети посреди закачки — `UpdateDownloader` даёт внятную ошибку,
      огрызок файла стёрт
- [ ] Чужая подпись — проверяется один раз руками на телефоне: читается через
      `PackageManager`, а тащить ради этого Robolectric не стоит
- [ ] Установка из RuStore (`adb install -i ru.vk.store`) — когда-нибудь потом

### Сплошное покрытие тестами

Правки статусов 01.09.2026 показали дыру: «пришёл» и «оплачено» разъехались,
а поймали это три упавших теста из трёхсот шестидесяти — остальные места,
где считаются деньги и «проведено», проверять нечем. Нужен разбор по
модулям: где поведение важное, там тест, а не надежда на ручную проверку
на телефоне.

- [ ] Разбор статусов и денег: `SessionParser`, `MonthRatesResolver`,
      `CalendarStatsCalculator`, `DaySummaryStats`, `IntensiveSessionUtils` —
      каждый статус (`0/1/2/3/4`) в каждом счётчике, включая старый формат
      `name|true` и интенсив с детьми и без
- [ ] Синхронизация с YClients: слияние с локальным днём, догон по
      `changed_after`, применение пуш-событий, расхождение с архивом
- [ ] Разбор и сериализация записей туда-обратно на всех форматах,
      включая поля с `|` внутри
- [ ] Список того, что осталось непокрытым, — в `docs/debt.md`, чтобы дыра
      была видна, а не забыта

### Бэклог

- [ ] Виджет «Расписание на сегодня»
- [ ] Мастер слияния при конфликте архив ↔ YClients
- [ ] Включить `lint.abortOnError` для release и закрыть критичные предупреждения

## Решено

- **Для кого приложение.** Задумано для любого специалиста центра, но пока
  работает один — расширять формат под 44 услуги и карту `service_id → ставка`
  сейчас незачем. Вернуться к вопросу, когда появится второй специалист
  (разбор — [`docs/pricing-from-api/GAPS.md`](docs/pricing-from-api/GAPS.md))

## Не планируется сейчас

- **PDF / отчёт за месяц** — не нужен, вычеркнут 13.08.2026
- **Compose UI-тесты** (календарь, drawer, диалог дня) — вычеркнуты
  13.08.2026: нужен эмулятор, идут минутами и ломаются от любой правки
  вёрстки. Логика проверяется обычными тестами, их 44
- **Проверка обновления на Android ниже 12** — не проверяем, версия неактуальна
- **Ротация `partner_token` YClients.** Ключ лежал в публичных APK версий до
  `0.1.0` и секретом не является. Решено 13.08.2026 не менять: в кабинете
  YClients перевыпуск не нашёлся, а риск владелец принимает осознанно —
  разбор остаётся в [`docs/updater/RISKS.md`](docs/updater/RISKS.md#секреты-в-публичном-apk)
- **Любая запись в YClients** — смена статуса, перенос, правка записи через
  API. У аккаунта нет прав на запись, и они не нужны: приложение только
  читает, ручные правки живут в архиве и наружу не уезжают
- **Периодическая синхронизация «каждые 4 часа»** — заменена на live-опрос
  (1 мин днём / 60 мин ночью МСК) и дневную автосинхронизацию при открытии

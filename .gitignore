*.iml
.gradle
.vscode/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/google-services.json
.signing/
ruStoreApi.txt
neiro-push-events/.env

# Локальные утилиты и выгрузки (не в репозитории)
tools/
scripts/icon/node_modules/
/app/release/
*.aab
*.apk

# Бэкапы push-сервисов: база и .env с секретами — только локально
neiro-push/backups/

# Погашенные поколения сервиса: код удалён из репозитория 13.08.2026 и лежит
# в истории git (коммит 5a95262). На диске остались только бэкапы БД, .env и
# secrets/ — единственное, что от них уцелело. В репозиторий им нельзя.
/server/
/neiro-push-events/

# Исходники скриншотов: полноразмерные кадры с телефона по мегабайту штука.
# В репозиторий уезжает только то, что собрал scripts/screenshots.sh.
docs/images/raw/

# Личные разрешения агента: пути к venv и прочее только для этой машины
.claude/settings.local.json
